# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Build commands

Do not run `npm run build` during routine development or after ordinary code changes. Only run it when the user explicitly requests a production build or build verification. Use focused checks such as TypeScript, lint, or relevant tests when appropriate.
